10 
 Figure 4 — Scatter plot of the sine/cosine encoding of typical_hour, confirming the 24-hour cycle is mapped onto a closed ring rather than a broken line. 4.2 Missing-value treatment and the loyalty_card_number decision Missing values are handled with domain-aware logic rather than a single blanket rule (e.g., zero-filling counts that are plausibly “no transactions of this type” versus model-based imputation for genuinely unknown continuous values), and KNN imputation is applied where the missingness looks closer to “unknown” than to “zero”. loyalty_card_number — confirmed in Notebook 0 to be ~40% missing and effectively a near-unique identifier — is converted into a simple binary customer_loyalty_flag, which keeps the useful behavioural signal (does this person have a loyalty card or not?) while discarding the useless identifier-like cardinality. 4.3 Aggregation and demographic feature engineering 
