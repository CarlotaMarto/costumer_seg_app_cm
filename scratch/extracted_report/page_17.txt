17 
 Figure 11 — Side-by-side UMAP projections comparing the “separated outliers” pipeline (info_clustering_unscaled.csv) and the “IQR-capped” pipeline (info_clustering_capped.csv). The notebook opens its modelling work with a direct visual comparison of the two candidate datasets exported from Notebook 1, projected with UMAP so that any difference in cluster geometry would be visible at a glance (Figure 11). The separated-outliers version produces a noticeably tighter, better-separated structure with a small, clearly distinct island of atypical points, while the capped version smears those same atypical customers back into the main mass, blurring the boundaries between groups. On the strength of this comparison the notebook commits to the separation-and-reattachment strategy (info_clustering_unscaled.csv as the base for fitting, with the 1,023 separated customers reattached after the fact) — a decision that is then carried through the rest of the notebook and is consistent with the “consensus outlier” reasoning documented in Notebook 1. 6.2 Scaler comparison: MinMax vs. Robust across a range of k 
 Figure 12 — Average silhouette score as a function of k, computed separately for MinMaxScaler and RobustScaler on the same candidate feature set. Figure 12 plots silhouette-vs-k curves for the two most defensible scaler choices for this kind of skewed, mixed-magnitude spend data. MinMaxScaler consistently sits above RobustScaler across the whole range of k tested, and both curves show the same qualitative shape — a peak in the low-to-mid k range followed by a gradual decline — which gives confidence that the 
