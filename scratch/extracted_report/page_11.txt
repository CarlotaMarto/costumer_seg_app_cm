11 
 Figure 5 — Distributions of the new household/engagement count features created in this step (e.g., total_children, tenure). 
 Figure 6 — Distribution of the engineered education-level proxy extracted from customer_name. This is where the project turns raw columns into modelling-ready variables: customer_age and tenure from the date fields, a binary gender flag, an ordinal education_years proxy parsed safely from customer_name with regex (Figure 6), kids_home and teens_home combined into a single 
