28 
 Figure 25b — Continuation of the KMeans-vs-Ward UMAP comparison (additional view / metrics). 
 Figure 26 — R² (between-cluster variance explained) comparison across hierarchical-linkage variants and the KMeans solution. Having committed to a configuration, the notebook then does something many student projects skip entirely: it goes looking for evidence that a *different* algorithm family might have done better. Figure 24 frames this as an “independent method search” — an attempt to validate KMeans's choice not just by tuning its own hyper-parameters, but by setting it against fundamentally different clustering paradigms on the same data. Figures 25a–b project a Ward-hierarchical solution (fitted on the same representative sample used for the dendrograms) into the same UMAP space as the KMeans labels, side by side — visually, the two partitions carve the data into broadly similar 
